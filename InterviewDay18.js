import { useCallback } from "react";

function InterviewDay18() {
  const add = useCallback(() => {}, []);
  const sub = useCallback(() => {}, []);

  return (
    <div>
      <User add={add} />
    </div>
  );
}
export default InterviewDay18;
